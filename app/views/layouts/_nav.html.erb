<nav>
  <div class="navbar navbar-inverse" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Coinflux", root_url, class: "navbar-brand" %>
        </div>


        <div class="collapse navbar-collapse pull-left">
          <ul class="nav navbar-nav">
            <% if current_user %>
              <li>
                <%= link_to_unless_selected "Wallets", wallets_path, { controller: "wallets", action: "index" }  %>
              </li>
              <li>
                <%= link_to_unless_selected "Ticker Prices", ticker_prices_path, { controller: "ticker_prices", action: "index" } %>
              </li>
              <li><%= link_to "Logout", session_path(0), method: :delete %></li>
            <% else %>
              <li><%= link_to "Register", new_user_path %></li>
              <li><%= link_to "Login", new_session_path %></li>
            <% end %>
          </ul>
        </div>

        <ul class="nav navbar-nav live-ticker pull-left">
          <li class='buy'>
          <%= link_to ticker_prices_path do %>
            Buy: <span><%= formatted_price @latest_price[:buy] %></span>
          <% end %>
          </li>
          <li class='sell'>
          <%= link_to ticker_prices_path do %>
            Sell: <span><%= formatted_price @latest_price[:sell] %></span>
          <% end %>
          </li>
        </ul>

      </div>
    </div>
</nav>
